node_modules
dist
dist-ssr
*.local
.DS_Store

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# Licensed system fonts used as test fixtures. Not redistributable; run
# scripts/fetch-test-fonts.sh to populate this locally. Tests that need a
# fixture skip when it is absent.
test-fonts/

# Generated by `npm run build:django`. The Vite source is the source of
# truth; build these before copying the app into a Django project.
server/font_studio/static/font_studio/
server/font_studio/templates/font_studio/app.html

# Django runtime state: the dev database and uploaded fonts.
server/db.sqlite3
server/media/
server/staticfiles/
__pycache__/
*.py[cod]

server/.venv/
server/.env
